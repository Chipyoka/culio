@page
@model IndexModel
@{
    ViewData["Title"] = "Know Your Sign";
}

<main>
       <h1 class="text-center mt-5 fw-bold">Culio</h1>
        <p class="text-center" id="intro">Enter Your month and day of birth to find out Your Zodiac Sign.</p>

    <section class="mb-3 d-flex justify-content-center mx-2 know">
        <div>

            <div class="text-left">
                <form asp-action="Index" method="post">
                     <label for="day">Day:</label>
                    <div class="input-group my-2">  
                        <input type="number" name="Day" id="day" class="form-control" min="1" max="31" placeholder="1">
                    </div>
                    <label for="month">Select Month:</label>

                    <select id="month" name="Month" class="form-select" aria-label="Default select example">
                        <option selected value="1">January</option>
                        <option value="2">February</option>
                        <option value="3">March</option>
                        <option value="4">April</option>
                        <option value="5">May</option>
                        <option value="6">June</option>
                        <option value="7">July</option>
                        <option value="8">August</option>
                        <option value="9">September</option>
                        <option value="10">October</option>
                        <option value="11">November</option>
                        <option value="12">December</option>
                    </select>

                    <div class="mt-2">
                        @* <button class="btn btn-secondary me-5 px-4" type="reset">Clear</button> *@
                        <button class="btn btn-primary px-4" type="submit">Find Sign</button>
                    </div>

                </form>
            </div>
            @if (!string.IsNullOrEmpty(Model.Sign) && !string.IsNullOrEmpty(Model.Desc))
            {
                    <div class="alert alert-info mt-4">
                        <h3>@Model.Sign</h3>
                        <p>@Model.Desc</p>
                    </div>
            }

        </div>
    </section>
    <section class="container d-flex justify-content-center story">
        <div class="mt-5 w-75 glass-morphism p-3">
            <h4>What are they? 🌟</h4>
            <p>
                Zodiac signs are like your cosmic personality traits, 
                with each one having its quirks. <u>Aries</u> is the fiery "let's do 
                this!" leader, while <u>Taurus</u> is the chill lover of snacks and naps. 
                <u>Gemini</u> is the chatty, two-faced buddy, and <u>Cancer's</u> all about hugging 
                it out. <u>Leo</u>? Total spotlight hog. <u>Virgo's</u> the overachiever who 
                organizes her sock drawer by color. <u>Libra's</u> the peacekeeper, 
                always playing referee. <u> Scorpio</u> is intense—like a mystery novel 
                in human form. <u>Sagittarius</u> loves adventure, <u>Capricorn</u> is the serious, 
                work-hard type. <u>Aquarius</u> is the quirky genius, and <u>Pisces</u>? Well, 
                they’re off dreaming somewhere. It’s a zodiac circus!
            </p>
        </div>
    </section>
</main>
